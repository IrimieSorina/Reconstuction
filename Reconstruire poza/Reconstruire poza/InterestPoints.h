#pragma once
#include "Stdafx.h"
using namespace std;

class InterestPoints
{
public:
	InterestPoints();
	~InterestPoints();

	/*void getGaussianDerivs(double sigma, int M, vector<double>& gaussian, vector<double>& dg, vector<double>& d2g);
	void getdX(vector<double> x,
		int n,
		double sigma,
		double& gx,
		double& dgx,
		double& d2gx,
		vector<double> g,
		vector<double> dg,
		vector<double> d2g,
		bool isOpen = false);
	void getdXcurve(vector<double> x,
		double sigma,
		vector<double>& gx,
		vector<double>& dx,
		vector<double>& d2x,
		vector<double> g,
		vector<double> dg,
		vector<double> d2g,
		bool isOpen = false);

	void PolyLineMerge(vector<Point2d>& pl, const vector<double>& contourx, const vector<double>& contoury);
    void PolyLineSplit(const vector<Point2d>& pl, vector<double>& contourx, vector<double>& contoury);

	void ResampleCurve(const vector<double>& curvex, const vector<double>& curvey,
		vector<double>& resampleX, vector<double>& resampleY,
		int N,
		bool isOpen
		);
	void ComputeCurveCSS(const vector<double>& curvex,
		const vector<double>& curvey,
		vector<double>& kappa,
		vector<double>& smoothX, vector<double>& smoothY,
		double sigma,
		bool isOpen
		);
	vector<int> FindCSSInterestPoints(const vector<double>& kappa);*/
};

